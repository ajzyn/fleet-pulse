export interface VehicleHeaderView {
  title: string;
  vin: string;
  year: number;
  fuelLabel: string;
  mileage: string;
  lastService: string;
  driver: string;
  purchaseDate: string;
  purchasePrice: string;
}
