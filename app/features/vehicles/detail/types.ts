export interface VehicleHeaderView {
  title: string;
  plateNumber: string;
  vin: string;
  year: number;
  fuelLabel: string;
  mileage: string;
  lastService: string;
  driver: string;
  purchaseDate: string;
  purchasePrice: string;
}
